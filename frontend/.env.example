# Copy this file to .env and adjust for your local setup.

# Base URL of the PitWall backend (see backend/.env.example — the FastAPI
# app listens on port 8000 by default via `uvicorn app.main:app --reload`).
VITE_API_BASE_URL=http://localhost:8000
